# Checklist de rutas (verificación visual)

Tras cambios de UI, layout o metadatos, revisar manualmente. Copy del sitio sin raya em en puntuación.


| Ruta            | Qué comprobar                                      |
|-----------------|-----------------------------------------------------|
| `/`             | Home, hero (foto de perfil circular centrada sobre un aro de vidrio, encima del nombre y en el lugar de la antigua etiqueta "Bogotá, Colombia"; 128 px en móvil y 144 px desde 768 px; `public/images/perfil-home.webp`), sección historia (#historia: foto + scrim, timeline con 6 hitos expandibles y línea que se dibuja con el scroll, fade antes de "Lo que hago"), sección "Lo que hago" (titular «Empieza por lo que necesitas hoy» y 3 tarjetas, una por servicio: ícono, para quién es, cifra clave, texto, fichas con proyectos, ofertas o planes y CTA; desde 768 px las filas quedan alineadas entre las tres; cada tarjeta lleva a su ruta; datos en `lib/home-disciplines.ts`), CTA, tema claro/oscuro |
| `/growth`       | Hero, imagen de sesiones en marco de terminal y selector de servicios (mini-cards con nombre, precio y duración; un solo panel de detalle debajo; en móvil la fila se desliza con dedo, rueda y trackpad; flechas, Inicio y Fin cambian de servicio; "Siguiente servicio" al final del panel). Sin hash → Meta Ads seleccionado; #metaads/#presencia-rrss/#control-marca-rrss → ese servicio y scroll al selector; "Ver ofertas" (#servicios) → selector; sesiones internas cerradas al abrir; hero/panel Meta Ads: «Agenda una llamada»; título «Entrenamiento Meta Ads» |
| `/filmmaker`    | Hero, foto de Jhonattan solo en móvil (< 768 px, sin texto; en escritorio y tablet desde 768 px no aparece, hasta definir su integración), planes (en móvil y tablet carrusel en bucle infinito que arranca centrado en el plan destacado, con dedo, mouse, rueda horizontal y teclado; desde `lg` cuadrícula de 3), proceso, portafolio de videos (lightbox), metodología (3 pasos Producir, Pautar, Llegar unidos por un trazo, y panel de visión estratégica con el framework Valor, Ventas, Blockbuster; el botón "Ver el entrenamiento Meta Ads" lleva a `/growth#metaads`), resultados, CTA |
| `/developer-ai` | Hero estilo terminal, stack, proyectos (galería de tarjetas con portada; bajo 768 px son tarjetas compactas al estilo LinkedIn: 1 columna bajo 360 px, 2 hasta 639 px y 3 desde 640 px, con etiqueta, título y resumen de 2 líneas, estado y botón "Ver detalles" de contorno, sin stack ni rol; desde 768 px, tarjetas grandes de 2 columnas; al tocar abre una hoja modal, en móvil un drawer inferior con arrastre; captura arriba, listas "cómo funciona" y "tecnología" alineadas, ESC devuelve el foco a la tarjeta), imagen de eventos en marco de terminal (desde 768 px entre proyectos y services; en móvil, encima del portafolio, justo después del héroe), services, sección filosofía (Kelly Johnson / Skunk Works) entre services y CTA, CTA |

Animaciones (`components/shared/reveal.tsx`, `typed-text.tsx`, `app/template.tsx`): al recorrer cada ruta no debe quedar nada oculto (anclas y recarga a media página incluidas), las etiquetas de terminal se escriben una vez, la línea de tiempo de Home se dibuja con el scroll, y con "reducir movimiento" todo aparece sin animar. Un `Reveal` nunca envuelve a un elemento con `backdrop-filter` (la clase va en ese mismo elemento) ni a las slides de Embla.

Modo Night (botón de luna en el navbar; móvil arriba a la derecha; `lib/night-mode.ts`, atributo `data-night` en `<html>`, preferencia en `localStorage["jr-night"]`): con Night activo, `/growth`, `/filmmaker` y `/developer-ai` pintan fondo `rgb(0, 0, 0)` en página, hero, footer, navbar con scroll y modales, sin resplandores de acento ni velos marrones en Film; `/` queda idéntico en ambos tonos. Con Night apagado todo se ve como antes. La preferencia sobrevive a la recarga (sin destello del tono actual) y a la navegación entre rutas.

Navegación móvil (< 768 px): píldora flotante redondeada abajo al centro, con el mismo diseño glass que la del navbar de escritorio (`components/nav/mobile-tab-bar.tsx`). Comprobar que el item activo se ve en píldora con el acento de la ruta, que nada la tapa, que el copyright del footer queda por encima al llegar al final y que a partir de 768 px desaparece y queda la píldora de escritorio.

Compartir e instalar: `lib/site-metadata.ts` (`SITE_URL` y `shareMetadata`) arma `og:*`, `twitter:*` y `canonical` de cada ruta, con la imagen de `public/og/<ruta>.jpg` (1200×630, retrato del hero sobre el color de la sección; `home`, `growth`, `developer-ai`, `filmmaker`). Íconos: `app/favicon.ico`, `app/icon.png`, `app/apple-icon.png`; manifest en `app/manifest.ts` con `public/icons/*` (192, 512 y maskable). Al cambiar un `.jpg` de `public/og/`, subir `SHARE_IMAGE_VERSION` en `lib/site-metadata.ts`: WhatsApp, Facebook y X guardan la imagen por URL y sin eso siguen mostrando la anterior. La página también se guarda por URL, así que para verla de nuevo probar el enlace con `?v=N`.

Incluye navegación desde el navbar (4 rutas: Home, Dev, Growth, Film) y el botón "CV" del hero de Home, que hace scroll a #historia.

Enlaces sociales: editar solo `lib/cta-links.ts` → `SOCIAL_PROFILES`. Botón del hero por sección: Dev = GitHub, Growth = LinkedIn, Film = YouTube (`ROUTE_HERO_SOCIAL`). Instagram queda reservado para la futura sección de coach y por ahora solo aparece en el footer.
